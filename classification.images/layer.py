from keras \
    import Model

import tensorflow \
    as tf

from tensorflow \
    import keras

import numpy \
    as np

from keras.models \
    import Sequential

from keras.layers \
    import *


#inputs = Input(shape=(32,))
#body = Dense(32)(inputs)

#model = Model(inputs, body)

#model.add(Dense(32))

#model.summary()


class Layer:
    def __init__(self):
        pass


